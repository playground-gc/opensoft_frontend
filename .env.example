# WebSocket server URL — the trading engine pushes market data over this connection.
VITE_WS_URL=ws://localhost:8001/ws

# Base URL for all REST API calls (order placement, portfolio snapshot, etc.).
VITE_API_BASE_URL=http://localhost:8000
