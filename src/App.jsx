/**
 * @file App.jsx
 * @description Root application component — configures React Router routes and top-level providers.
 * Renders <RootLayout> as the shell and lazy-loads page-level components.
 * @exports default App  React component
 * @note Route-level code splitting via React.lazy keeps the initial bundle small.
 */

export default {};
