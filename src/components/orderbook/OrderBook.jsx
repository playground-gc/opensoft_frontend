/**
 * @file OrderBook.jsx
 * @description Container that assembles the full limit order book widget: asks table on top,
 * SpreadIndicator in the middle, and bids table below — mirroring standard exchange layouts.
 * @exports default OrderBook  React component
 * @note Passes a stable depth-level array from useOrderBook to avoid child re-renders on unrelated updates.
 */

export default function OrderBook() {
  return null;
}
