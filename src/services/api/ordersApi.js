/**
 * @file ordersApi.js
 * @description REST API functions for order management: place a new order and cancel an existing one.
 * All functions return Axios promise chains and throw on non-2xx responses.
 * @exports placeOrder, cancelOrder
 * @note Used by useOrderSubmit hook; React Query mutation wrappers live in the hook, not here.
 */

export const placeOrder = () => Promise.resolve();

export const cancelOrder = () => Promise.resolve();
